import { Router } from 'express'; import login from '../login.js'; import register from '../register.js'; const router=Router(); router.post('/login',login); router.post('/register',register); export default router;
