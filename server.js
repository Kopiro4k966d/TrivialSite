import 'dotenv/config'; import express from 'express'; import cors from 'cors'; import path from 'path'; import { fileURLToPath } from 'url'; import auth from './api/routes/auth.js'; import account from './api/routes/account.js'; import admin from './api/routes/admin.js'; import configs from './api/routes/configs.js'; import payment from './api/routes/payment.js';
const app=express();const root=path.dirname(fileURLToPath(import.meta.url));app.use(cors());app.use(express.json({limit:'1mb'}));app.use('/api',auth,account,admin,configs,payment);app.use(express.static(root));const pages={home:'index.html',signin:'signin.html',signup:'signup.html',purchase:'purchase.html',profile:'profile.html','public-offer':'public-offer.html'};Object.entries(pages).forEach(([route,file])=>app.get('/'+route,(_req,res)=>res.sendFile(path.join(root,file))));app.listen(process.env.PORT||3000,()=>console.log(`Trivialsite: http://localhost:${process.env.PORT||3000}`));
