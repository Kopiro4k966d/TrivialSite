import pool from './db.js'; import { generateKey } from './utils/generateKeys.js';
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({success:false,message:'Method not allowed'});const username=String(req.body?.username||'');const duration=Math.max(1,Math.min(365,Number(req.body?.duration)||30));try{const check=await pool.query('SELECT role FROM users WHERE username=$1',[username]);if(!check.rows.length||check.rows[0].role!=='creator')return res.status(403).json({success:false,message:'Недостаточно прав'});const key=generateKey(duration);await pool.query('INSERT INTO licenses (license_key,status,duration_days) VALUES ($1,$2,$3)',[key,'unused',duration]);return res.json({success:true,key});}catch(error){console.error(error);return res.status(500).json({success:false,message:'Ошибка создания ключа'});}}
